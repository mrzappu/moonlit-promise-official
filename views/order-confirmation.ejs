<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Confirmation - SportsWear</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        :root {
            --primary-color: #2c3e50;
            --secondary-color: #e74c3c;
            --accent-color: #3498db;
            --success-color: #27ae60;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f8f9fa;
        }
        
        .confirmation-container {
            max-width: 800px;
            margin: 50px auto;
            background: white;
            border-radius: 20px;
            padding: 40px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            text-align: center;
        }
        
        .success-icon {
            width: 100px;
            height: 100px;
            background: var(--success-color);
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 3rem;
            margin: 0 auto 30px;
        }
        
        .order-number {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--primary-color);
            margin-bottom: 10px;
        }
        
        .order-date {
            color: #666;
            margin-bottom: 30px;
        }
        
        .order-details {
            background: #f8f9fa;
            border-radius: 15px;
            padding: 30px;
            margin: 30px 0;
            text-align: left;
        }
        
        .detail-row {
            display: flex;
            justify-content: space-between;
            padding: 10px 0;
            border-bottom: 1px solid #e0e0e0;
        }
        
        .detail-row:last-child {
            border-bottom: none;
        }
        
        .detail-label {
            font-weight: 600;
            color: var(--primary-color);
        }
        
        .detail-value {
            color: #666;
        }
        
        .btn-home {
            background: var(--primary-color);
            color: white;
            border: none;
            padding: 12px 30px;
            border-radius: 30px;
            font-weight: 600;
            text-decoration: none;
            display: inline-block;
            margin: 10px;
            transition: all 0.3s;
        }
        
        .btn-home:hover {
            background: #34495e;
            transform: translateY(-2px);
            color: white;
        }
        
        .btn-shop {
            background: var(--accent-color);
            color: white;
            border: none;
            padding: 12px 30px;
            border-radius: 30px;
            font-weight: 600;
            text-decoration: none;
            display: inline-block;
            margin: 10px;
            transition: all 0.3s;
        }
        
        .btn-shop:hover {
            background: #2980b9;
            transform: translateY(-2px);
            color: white;
        }
        
        .whatsapp-share {
            background: #25D366;
            color: white;
            padding: 12px 30px;
            border-radius: 30px;
            text-decoration: none;
            display: inline-block;
            margin: 10px;
            transition: all 0.3s;
        }
        
        .whatsapp-share:hover {
            background: #128C7E;
            color: white;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="confirmation-container">
            <div class="success-icon">
                <i class="fas fa-check"></i>
            </div>
            
            <h1>Order Confirmed!</h1>
            <p class="order-number">Order #<%= order.order_number %></p>
            <p class="order-date">Placed on <%= new Date(order.created_at).toLocaleString() %></p>
            
            <div class="order-details">
                <h4 class="mb-3">Order Summary</h4>
                
                <% orderItems.forEach(item => { %>
                    <div class="detail-row">
                        <span class="detail-label"><%= item.name %> x<%= item.quantity %></span>
                        <span class="detail-value">₹<%= item.price * item.quantity %></span>
                    </div>
                <% }); %>
                
                <div class="detail-row">
                    <span class="detail-label">Subtotal</span>
                    <span class="detail-value">₹<%= order.total_amount %></span>
                </div>
                
                <div class="detail-row">
                    <span class="detail-label">Payment Method</span>
                    <span class="detail-value"><%= order.payment_method %></span>
                </div>
                
                <div class="detail-row">
                    <span class="detail-label">Order Status</span>
                    <span class="detail-value"><span class="badge bg-warning"><%= order.status %></span></span>
                </div>
            </div>
            
            <p class="mb-4">Thank you for your purchase! We'll notify you once your order ships.</p>
            
            <div>
                <a href="/" class="btn-home">
                    <i class="fas fa-home me-2"></i>Go Home
                </a>
                <a href="/history" class="btn-shop">
                    <i class="fas fa-history me-2"></i>View Orders
                </a>
                <a href="https://wa.me/?text=My%20order%20<%= order.order_number %>%20is%20confirmed!%20Total:%20₹<%= order.total_amount %>" target="_blank" class="whatsapp-share">
                    <i class="fab fa-whatsapp me-2"></i>Share on WhatsApp
                </a>
            </div>
        </div>
    </div>
</body>
</html>
