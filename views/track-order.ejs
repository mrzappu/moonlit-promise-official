<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Track Order - SportsWear</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        :root {
            --primary-color: #2c3e50;
            --success-color: #27ae60;
        }
        
        body {
            font-family: 'Segoe UI', sans-serif;
            background: #f8f9fa;
        }
        
        .track-container {
            max-width: 600px;
            margin: 50px auto;
            background: white;
            border-radius: 20px;
            padding: 40px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        }
        
        .track-status {
            display: flex;
            justify-content: space-between;
            margin: 40px 0;
            position: relative;
        }
        
        .track-status:before {
            content: '';
            position: absolute;
            top: 25px;
            left: 0;
            right: 0;
            height: 2px;
            background: #e0e0e0;
            z-index: 1;
        }
        
        .status-item {
            text-align: center;
            position: relative;
            z-index: 2;
            flex: 1;
        }
        
        .status-dot {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            background: white;
            border: 2px solid #e0e0e0;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 10px;
            font-weight: 700;
        }
        
        .status-item.completed .status-dot {
            background: var(--success-color);
            border-color: var(--success-color);
            color: white;
        }
        
        .status-label {
            font-size: 0.9rem;
            color: #666;
        }
        
        .status-item.completed .status-label {
            color: var(--success-color);
            font-weight: 600;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="track-container">
            <h2 class="text-center mb-4">Track Order</h2>
            <p class="text-center">Order #<%= order.order_number %></p>
            
            <div class="track-status">
                <% trackingStatus.forEach((status, index) => { %>
                    <div class="status-item <%= status.completed ? 'completed' : '' %>">
                        <div class="status-dot"><%= index + 1 %></div>
                        <div class="status-label"><%= status.status %></div>
                        <% if (status.date) { %>
                            <small class="text-muted"><%= new Date(status.date).toLocaleDateString() %></small>
                        <% } %>
                    </div>
                <% }); %>
            </div>
            
            <div class="text-center mt-4">
                <a href="/history" class="btn btn-primary">Back to Orders</a>
            </div>
        </div>
    </div>
</body>
</html>
